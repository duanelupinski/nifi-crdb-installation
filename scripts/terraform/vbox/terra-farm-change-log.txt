updates to vbox terraform provider at https://registry.terraform.io/providers/terra-farm/virtualbox/0.2.2-alpha.1



/****************************************************************************************************************/



jleelong@hutchandben:~/workspace/terraform-provider-virtualbox$ git diff examples/example.tf
diff --git a/examples/example.tf b/examples/example.tf
index 491122f..75412c4 100644
--- a/examples/example.tf
+++ b/examples/example.tf
@@ -1,3 +1,12 @@
+terraform {
+  required_providers {
+    virtualbox = {
+      source = "registry.local/local/virtualbox",
+      version = "5.0.0"
+    }
+  }
+}
+
 resource "virtualbox_vm" "node" {
   count     = 2
   name      = format("node-%02d", count.index + 1)
@@ -7,7 +16,7 @@ resource "virtualbox_vm" "node" {

   network_adapter {
     type           = "hostonly"
-    host_interface = "vboxnet1"
+    host_interface = "vboxnet0"
   }
 }



/****************************************************************************************************************/



jleelong@hutchandben:~/workspace/terraform-provider-virtualbox$ git diff go.mod
diff --git a/go.mod b/go.mod
index ce60843..dde6665 100644
--- a/go.mod
+++ b/go.mod
@@ -56,7 +56,7 @@ require (
        golang.org/x/net v0.0.0-20220812174116-3211cb980234 // indirect
        golang.org/x/sys v0.0.0-20220817070843-5a390386f1f2 // indirect
        golang.org/x/text v0.3.8 // indirect
-       google.golang.org/appengine v1.6.7 // indirect
+       google.golang.org/appengine v1.6.6 // indirect
        google.golang.org/genproto v0.0.0-20220817144833-d7fd3f11b9b1 // indirect
        google.golang.org/grpc v1.48.0 // indirect
        google.golang.org/protobuf v1.28.1 // indirect



/****************************************************************************************************************/



jleelong@hutchandben:~/workspace/terraform-provider-virtualbox$ git diff go.sum
diff --git a/go.sum b/go.sum
index 1933a33..5ab4eb5 100644
--- a/go.sum
+++ b/go.sum
@@ -313,8 +313,8 @@ golang.org/x/xerrors v0.0.0-20200804184101-5ec99f83aff1/go.mod h1:I/5z698sn9Ka8T
 google.golang.org/appengine v1.1.0/go.mod h1:EbEs0AVv82hx2wNQdGPgUI5lhzA/G0D9YwlJXL52JkM=
 google.golang.org/appengine v1.4.0/go.mod h1:xpcJRLb0r/rnEns0DIKYYv+WjYCduHsrkT7/EB5XEv4=
 google.golang.org/appengine v1.6.5/go.mod h1:8WjMMxjGQR8xUklV/ARdw2HLXBOI7O7uCIDZVag1xfc=
-google.golang.org/appengine v1.6.7 h1:FZR1q0exgwxzPzp/aF+VccGrSfxfPpkBqjIIEq3ru6c=
-google.golang.org/appengine v1.6.7/go.mod h1:8WjMMxjGQR8xUklV/ARdw2HLXBOI7O7uCIDZVag1xfc=
+google.golang.org/appengine v1.6.6 h1:lMO5rYAqUxkmaj76jAkRUvt5JZgFymx/+Q5Mzfivuhc=
+google.golang.org/appengine v1.6.6/go.mod h1:8WjMMxjGQR8xUklV/ARdw2HLXBOI7O7uCIDZVag1xfc=
 google.golang.org/genproto v0.0.0-20180817151627-c66870c02cf8/go.mod h1:JiN7NxoALGmiZfu7CAH4rXhgtRTLTxftemlI0sWmxmc=
 google.golang.org/genproto v0.0.0-20190819201941-24fa4b261c55/go.mod h1:DMBHOl98Agz4BDEuKkezgsaosCRResVns1a3J2ZsMNc=
 google.golang.org/genproto v0.0.0-20200513103714-09dca8ec2884/go.mod h1:55QSHmfGQM9UVYDPBsyGGes0y52j32PQ3BqQfXhyH3c=



/****************************************************************************************************************/



jleelong@hutchandben:~/workspace/terraform-provider-virtualbox$ git diff internal/provider/resource_vm.go
diff --git a/internal/provider/resource_vm.go b/internal/provider/resource_vm.go
index 25aac93..f3e0745 100644
--- a/internal/provider/resource_vm.go
+++ b/internal/provider/resource_vm.go
@@ -1,14 +1,16 @@
+
 package provider

 import (
        "context"
        "fmt"
+       "sort"
        "io"
        "net/http"
        "net/url"
        "os"
-       "os/user"
        "path/filepath"
+       "regexp"
        "strconv"
        "strings"
        "sync"
@@ -176,6 +178,11 @@ func resourceVMExists(d *schema.ResourceData, meta any) (bool, error) {
 var imageOpMutex sync.Mutex

 func resourceVMCreate(ctx context.Context, d *schema.ResourceData, meta any) diag.Diagnostics {
+       tflog.Debug(ctx, "Creating VM Resource", map[string]any{
+               "data": fmt.Sprintf("%+v", d),
+               "meta": fmt.Sprintf("%+v", meta),
+       })
+
        image := d.Get("image").(string)

        if addr, exists := d.GetOk("url"); exists {
@@ -192,13 +199,13 @@ func resourceVMCreate(ctx context.Context, d *schema.ResourceData, meta any) dia
                return diag.Errorf("unable to fetch remote image: %v", err)
        }

-       /* Get gold folder and machine folder */
+       /* Get gold folder and machine folder 
        usr, err := user.Current()
        if err != nil {
                return diag.Errorf("unable to get the current user: %v", err)
-       }
-       goldFolder := filepath.Join(usr.HomeDir, ".terraform/virtualbox/gold")
-       machineFolder := filepath.Join(usr.HomeDir, ".terraform/virtualbox/machine")
+       } */
+       goldFolder := filepath.Join("/mnt/vbox/", ".terraform/virtualbox/gold")
+       machineFolder := filepath.Join("/mnt/vbox/", ".terraform/virtualbox/machine")
        err = os.MkdirAll(goldFolder, 0740)
        if err != nil {
                return diag.Errorf("unable to create gold folder: %v", err)
@@ -250,7 +257,7 @@ func resourceVMCreate(ctx context.Context, d *schema.ResourceData, meta any) dia
                err := vbox.CloneHD(src, target)
                imageOpMutex.Unlock()
                if err != nil {
-                       return diag.Errorf("failed to clone *.vdi and *.vmdk to VM folder: %v", err)
+                       return diag.Errorf("failed to clone *.vdi and *.vmdk to VM folder " + target + " from " + src + ": %v", err)
                }
        }

@@ -259,6 +266,10 @@ func resourceVMCreate(ctx context.Context, d *schema.ResourceData, meta any) dia
        if err != nil {
                return diag.Errorf("unable to gather disks: %v", err)
        }
+       sort.Strings(vmDisks)
+       tflog.Debug(ctx, "Attaching VM Disks", map[string]any{
+                       "list": fmt.Sprintf("%+v", vmDisks),
+       })

        if err := vm.AddStorageCtl("SATA", vbox.StorageController{
                SysBus:      vbox.SysBusSATA,
@@ -270,9 +281,22 @@ func resourceVMCreate(ctx context.Context, d *schema.ResourceData, meta any) dia
                return diag.Errorf("can't create VirtualBox storage controller: %v", err)
        }

+       // attaching disk to port based on disk number, assuming boot disk will be first and disk name includes the disk number
+       re := regexp.MustCompile("[0-9]+")
        for i, disk := range vmDisks {
+               matches := re.FindAllString(disk, 10)
+               diskNum, err := strconv.Atoi(strings.TrimLeft(matches[len(matches)-1], "0"))
+               if err != nil {
+                       return diag.Errorf("unable to parse disk number from name: %v", err)
+               }
+               tflog.Debug(ctx, "attaching disk to port", map[string]any{
+                       "i": fmt.Sprintf("%d", i),
+                       "number": fmt.Sprintf("%d", diskNum),
+                       "disk": disk,
+                       "port": fmt.Sprintf("%d", diskNum - 1),
+               })
                if err := vm.AttachStorage("SATA", vbox.StorageMedium{
-                       Port:      uint(i),
+                       Port:      uint(diskNum - 1),
                        Device:    0,
                        DriveType: vbox.DriveHDD,
                        Medium:    disk,
@@ -520,7 +544,7 @@ func waitUntilVMIsReady(ctx context.Context, d *schema.ResourceData, vm *vbox.Ma
                        []string{"no"},
                        key,
                        meta,
-                       30*time.Second,
+                       180*time.Second,
                        1*time.Second,
                ); err != nil {
                        return fmt.Errorf("waiting for VM (%s) to become ready: %w", d.Get("name"), err)



/****************************************************************************************************************/



